<%= flash_message :error %>

<%= form_tag scenario_path(@scenario), :method => :put do %>
  <%= render :partial => "form" %>
<% end -%>

<% if can?(:manage, @project => Scenario) %>
  <%= link_to 'Destroy', scenario_path(@scenario), :confirm => 'Are you sure?', :method => :delete, :class => "button" -%>
<% end %>
